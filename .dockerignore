# Git and development files
.git
.gitignore
.vscode
.idea
.agent
.agents
*.md
LICENSE

# Python
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.pyd
.Python
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyright_cache/
.coverage
htmlcov/
.testmondata

# Virtual environments (Exclude all .venv folders)
**/.venv/
**/venv/
**/env/
**/bin/

# Node.js
**/node_modules/
**/npm-debug.log*

# Build artifacts
# Note: frontend/dist is REQUIRED for the backend build, so we don't ignore all dist/
# Only ignore dist in specific places if needed, but the Dockerfile handles what it copies.
**/build/
**/*.egg-info/

# Local data and secrets (Important for size and security)
**/.env
local-files/
models/
storage/
**/*.db
**/*.sqlite3
**/*.log

# Infrastructure
infrastructure/terraform.tfstate*
infrastructure/.terraform/

# macOS
.DS_Store
