{
  "name": "paperterrace-api-gateway",
  "version": "1.0.0",
  "description": "Cloudflare Workers API Gateway for PaperTerrace",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js --format=esm",
    "test": "vitest",
    "type-check": "tsc --noEmit"
  },
  "keywords": ["cloudflare", "workers", "api-gateway"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240129.0",
    "@types/node": "^20.11.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0",
    "wrangler": "^3.25.0"
  }
}
