Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To check the status of your deployment, run:
  kubectl get pods -n {{ .Release.Namespace }} -w

To update your deployment with a new image tag:
  helm upgrade {{ .Release.Name }} ./infrastructure/helm/paperterrace --set backend.image.tag=new-tag --reuse-values

Redis is available at:
  {{ .Release.Name }}-redis-master:6379

Enjoy!
