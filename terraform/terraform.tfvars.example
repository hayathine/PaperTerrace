# Example terraform.tfvars file
# Copy this to terraform.tfvars and fill in your values

project_id     = "paperterrace"
project_number = "316073929194"
region         = "asia-northeast1"

# Docker image URL (updated by CI/CD)
image_url = "asia-northeast1-docker.pkg.dev/paperterrace/paperterrace/app:latest"

# Sensitive values - set via environment or secrets
# gemini_api_key = "your-gemini-api-key"
# db_password    = "your-secure-password"
